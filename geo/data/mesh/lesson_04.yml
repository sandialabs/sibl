--- # (optional) denotes beginning of document
version: 1.0  # float, the yml file input version
boundary: "~/sibl/geo/data/boundary/circle_radius_2.txt"
mesh:
  # bounding_box: [[lower_left_x, lower_left_y], [upper_right_x, upper_right_y]]
  bounding_box: [[0.0, 1.0], [3.0, 4.0]]  # [[float, float], [float, float]]
  resolution: 1.0 # float
  output_file: "~/sibl/geo/data/mesh/lesson_04.inp"
  boundary_refine: True  # bool, if False, then does a feature refine
  # always one or the other, boundary_refine has quad tree refine to parameteric 
  # representation of curve, and feature_refine is based on curvature.
  # 
  # boundary_refine: bool  # if boundary_refine, then it is not a feature_refine
  # read_boundary: bool
  # file_name_boundary: str  # .csv file
  # path_to_boundary: str
  # point_cloud boundary is discrete (not interpolated)
  
  # output_path: Union[str, Path]
  # 
  # write_dual_mesh: bool
  # file_name_mesh: str
  # file_name_nodes: str
  # file_name_elements: str  # formerly mesh_quads
  #
  # write_primal_mesh: bool
  # file_name_primal_mesh: str
  # file_name_primal_mesh_nodes: str
  # file_name_primal_mesh_elements: str
... # (optional) denotes end of the document
